count([],0).
count([_|T],X):-
    count(T,X1), X is X1 + 1.
    
sum([],0).
sum([H|T],X):-
    sum(T,X1), X is H + X1.
mul([],1).
mul([H|T],X):-
    mul(T,X1), X is H * X1.

max_e([H],H).
max_e([H|T],X):-
    max_e(T,X1), X is max(H,X1). 

min_e([H],H).
min_e([H|T],X):-
    min_e(T,X1), X is min(H,X1). 

concat_l([],L,L).
concat_l([H|T],L,[H|R]):-
    concat_l(T,L,R).

contain_e([X|_],X).
contain_e([_|T],A):-
    contain_e(T,A1), A is A1.

sum_even([],0).
sum_even([H|T],X):-
    sum_even(T,X1), (H mod 2 =:= 0 -> X is X1 + H; X = X1). 


csc([]).
csc([_]).
csc([A,B|T]):-
    D is B - A,
    D \= 0,
    check_csc([B|T],D).

check_csc([_],_).
check_csc([A,B|T],D):-
    B - A =:= D,
    check_csc([B|T],D).

csn([]).
csn([_]).
csn([A,B|T]):-
    D is B/A,
    check_csn([B|T],D).
check_csn([_],_).
check_csn([A,B|T],D):-
    D =:= B / A,
    D \= 0,
    A \= 0,
    check_csn([B|T],D).

contain_el([H|_],H).
contain_el([_|T],A):-
    contain_el(T,A).


intersect([],_,[]).
intersect([H|T],L2,[H|R]):-
    member(H,L2),
    intersect(T,L2,R).
intersect([_|T],L2,Res):-
    intersect(T,L2,Res).

union([],L,L).
union([H|T],L,Res):-
    member(H,L),
    union(T,L,Res).
union([H|T],L,[H|Res]):-
    \+ member(H,L),
    union(T,L,Res).


count_even([],0).
count_even([H|T],X):-
    count_even(T,X1), (H mod 2 =:= 0 -> X is X1 + 1; X is X1).

count_odd([],0).
count_odd([H|T],X):-
    count_odd(T,X1), (H mod 2 =:= 1 ->  X is X1 + 1; X is X1).

append_list([],L,L).
append_list([H|T],L,[H|R]):-
    append_list(T,L,R).

find_max([X],X).
find_max([H|T],X):-
    find_max(T,X1), X is max(H,X1).

sum_l([],0).
sum_l([X],X).
sum_l([H|T],X):-
    sum_l(T,X1), X is X1 + H.


is_even(N):- N mod 2 =:= 0.
filter_even([],[]).
filter_even([H|T],[H|Res]):-
    is_even(H),
    filter_even(T,Res).
filter_even([H|T],Res):-
    \+ is_even(H),
    filter_even(T,Res).


compressed([],[]).
compressed([X,X|T],Res):-
    compressed([X|T],Res).

compressed([X,Y|T],[X|Res]):-
    X\=Y,
    compressed([Y|T],Res).
compressed([X],[X]).
    

    